package lab5.demchuk.client.clientjavafx.model.Factory;

import com.google.gson.Gson;

public class ReversalObject {
    public static TypeCommand getObject(String str) {
        Gson gson = new Gson();
        //TypeCommand command = gson.fromJson(str, TypeCommand.class);
        //return command;
        return null;
    }
}
